# Jay README

## General
- Generally avoid using AI agents for initial setup. Setup manually.
- Very rarely you will use code generated by claude directly. It will only be useful by heavily influencing it.

## Context Window

- Every chat will include text from Claude.md, previous prompts and responses.
- Large context will muddle model and also increase token cost.
- Always /clear context before starting unrelated tasks

## Project setup

### Create Next.js Project
npx create-next-app@latest liftingdairyjay
npm run dev will launch web app at http://localhost:3000

### Setup shadcn ui for UI library
npx shadcn@latest init 

### Setup clerk for user authentication (Refer clerkAIprompt)

https://dashboard.clerk.com/: GoogleSignin (jaytestnut)

Run the AI Agent snipet from clerk. This will perform the below steps and will allow user Signup/Signin

Clerk CLI installed locally, signed in as jaytestnut@gmail.com
Project initialized and linked to app app_3IsJ6JzC77qoLGYvEGYrRv1oNEa (Next.js App Router detected)
src/proxy.ts, sign-in/sign-up pages, and .env.local keys created by clerk init
Added /__clerk/:path* to the proxy matcher
Added sign-in/sign-up buttons and a user button to layout.tsx header
clerk doctor passed all checks (dev instance configured; no production instance yet)
Dev server running at http://localhost:3000 — home, /sign-in, /sign-up all return 200

As a side effect it also installed 8 skills at ~/.agents/skills/clerk-*

**Clerk User**
jmuthialu@yahoo.com/WelcomeJayNext10

### Setup Neon postgress DB

https://console.neon.tech/; GoogleSignin (jaytestnut)

- We will not create DB tables directly in neon. We will use `drizzle` to create schema using Claude and push through Next.js to create tables in Neon.

### Setup drizzle

https://orm.drizzle.team
- Select Neon as our DB 
- Perform the steps manually given here https://orm.drizzle.team/docs/get-started/neon-new
- Note: DB Schems will be generated by Claude AND MCP

### Generate DB scheme using Claude
Plan mode with back and forth prompting
[prompt 1]
This is a workout logging app. Plan a table schema to log workouts. Each workout can have multiple exercises, and each exercise can have multiple sets.  make sure this is normalized. schema must be created using drizzle for a Postgres database hosted on neon

[prompt 2]
This plan looks good but needs few tweaks. Add a started at and completed at column for the workouts table. Also remove the notes.

[prompt 3]
For exercises table remove category and add updatedAt column. Also for workoutExercises add updatedAt column

[prompt 4]
implement the plan and push the scheme to neon.


## Generate seed data in Neon DB using Claude MCP
**MCP Basics**
MCP allows a model to connect to a 3rd party service (aka tool) and perform actions.

Neon Doc: https://neon.com/docs/ai/connect-mcp-clients-to-neon
npx add-mcp https://mcp.neon.tech/mcp
This adds .mcp.json file 

**Authenticate Neon MCP**
Go to terminal and type claude. MCP authentication seems to work only from terminal.

/mcp
Select neon and authorize
If sucessful /mcp will say neon is connected.

[prompt1]
list all tables in neon from liftingdairyjay project

[prompt2]
looks good, insert data into neon


## Create Dashboard feature
[prompt 1]
Create a document under docs/ui.md that outlines the coding standards for building UI layer for this app. All ui components should use only shadcn UI library. No custom UI allowed, only shadcn UI components.

For data formatting use the library date-fns. Date should be formatted like below examples.
1st Aug 2025
2nd Sep 2025
3rd Aug 2026
4th Feb 2026

[prompt 2]
Update claude.md file to ALWAYS refer to coding standards from the documents in docs folder.

[prompt 3]
Create /dashboard route that shows a dashboard feature which shows a calendar defaulting to current date and showing the workouts for that date. If user selects a different data it should  show the workouts for the selected date. Generate code for UI layer only. Do not generate any data fetching or server side code.