# Subagent

[prompt 1]
create a subagent that does the following. whenever a new documentation file is added to the /docs directory update CLAUDE.md file to reference this new file under ## Coding standards section.

This will create an agent under .claude/agents.

## Testing
/create-docs routing All routes in this app should be accessed via /dashboard. The /dashboard page and any sub pages should be protected routes that are only accessible by logged in users.

This will create routing.md and sub agent will update CLAUDE.md